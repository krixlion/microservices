apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: eventstore
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/eventstore: "true"
  podSelector:
    matchLabels:
      io.kompose.network/eventstore: "true"

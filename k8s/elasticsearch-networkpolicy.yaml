apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: elasticsearch
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/elasticsearch: "true"
  podSelector:
    matchLabels:
      io.kompose.network/elasticsearch: "true"

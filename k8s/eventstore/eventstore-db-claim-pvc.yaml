apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: eventstore-claim
  name: eventstore-db-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
